# Secrets / local config
server/runtime/.env
.env
.env.local
.env.*.local
clients/pwa/.env.local
site/.env.local
# Telegram-skill creds + Pyrogram session files. Creds live in
# <persona>/data/secrets/telegram/values.json (covered by the
# server/personas/*/data/ ignore below). Session files match anywhere.
*.session
*.session-journal

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/
dist/
build/

# Python tooling
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Runtime
*.db
*.db-shm
*.db-wal
server/runtime/logs/
logs/

# Persona-local data (audiobook library, sqlite DB) — large, user-provided
server/personas/*/data/
server/runtime/models/

# macOS
.DS_Store

# PWA client (clients/pwa)
clients/pwa/node_modules/
clients/pwa/dist/
clients/pwa/build/

# Marketing site (site/)
site/node_modules/
site/dist/
site/build/
